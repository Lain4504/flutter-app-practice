import 'package:flutter/material.dart';

// Light theme colors (Instagram-like)
const mobileBackgroundColor = Color.fromRGBO(255, 255, 255, 1); // White background
const webBackgroundColor = Color.fromRGBO(245, 245, 245, 1);     // Very light gray background
const mobileSearchColor = Color.fromRGBO(239, 239, 239, 1);      // Light gray for search box
const blueColor = Color.fromRGBO(0, 149, 246, 1);                 // Instagram blue remains the same
const primaryColor = Colors.black;                               // Text color: black in light theme
const secondaryColor = Colors.grey;                              // Secondary text or icons
